{
  "name": "cli",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {},
  "scripts": {
    "run:example-recursive": "yarn clean:examples && npx @autogen-export/cli -r './examples/recursiveGenerateExportFile'",
    "run:example": "yarn clean:examples && npx @autogen-export/cli './examples/generateExportFile/target/'",
    "clean:examples": "find './examples' | grep -E 'index.ts' | xargs -I % rm %"
  },
  "devDependencies": {
    "@autogen-export/cli": "^4.0.1"
  }
}
